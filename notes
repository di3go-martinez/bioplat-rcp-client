
Pasos para integrar la aplicación

1. una vez hecho el checkout de bioplat-rcp-client

1.- Descargar el módulo de integración
      git submodule init && git submodule update
2.- Descomprimir el archivo clean-rcp-oxygen.tar.gz que está dentro de bioplat-rcp-integration
      tar xf clean-rcp-oxygen.tar.gz
2.- ejecutar el script build.
      La versión se calcula segun git. si hay tag se utiliza como versión, sino se utiliza la fecha y el id del commit
